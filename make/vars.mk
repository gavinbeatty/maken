prefix = /usr/local
DESTDIR =

RM = rm -f
TAR = tar
ZIP = zip
INSTALL_DATA = install -m 0644
INSTALL_BIN = install -m 0755
INSTALL_DIR = install -d -m 0755
GIT = git
BZIP2 = bzip2
ASCIIDOC = asciidoc
A2X = a2x
SED = sed
MKDIR = mkdir -p --
TOUCH = touch

